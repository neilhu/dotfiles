# Keep connections alive and prevent timeouts
Host *
    ServerAliveInterval 60
    ServerAliveCountMax 3

    # Add keys to agent on first use so you only enter the passphrase once
    AddKeysToAgent yes

    # Only use explicitly listed identities (avoids leaking key existence)
    IdentitiesOnly yes
